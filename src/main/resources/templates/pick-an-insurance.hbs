{{#partial "content"}}
      <div class="content transparent">
        <h2 class="bvis-1 title">Pick an insurance policy</h2>
        <table class="table insurance-list">
          {{#each offerings}}
          <tr>
            <td class="name">{{name}}</td>
            <td class="price">€{{price}} <small>per day</small></td>
            <td >{{description}} </td>
            <td class="select">
              <form action="/{{processInstanceId}}/pick-an-insurance" method="POST">
                <input name="offerId" type="hidden" type="number" value="{{offer_id}}">
                <button type="submit" class="bvis-1-bg bvis-1-bg-hover button">Cover your rent</button>
              </form>
          </td>
          </tr>
          {{/each}}
        </table>
        <p class="right"><small>powered by</small> Capitol&reg;</p>
      </div>
{{/partial}}

{{> base}}